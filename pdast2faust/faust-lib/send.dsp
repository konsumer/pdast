// PD: send / s <name>  — send to a named receive bus (no outlet)
// The generator resolves send/receive pairs; send nodes become signal sinks.
// This stub drops the signal (equivalent of send with no paired receive).
pdobj = !;
